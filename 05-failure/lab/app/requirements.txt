# Layer 5 lab - the service under test. Pinned to majors, not to patches:
# the point of the lab is the queueing, not a dependency archaeology exercise.
fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
asyncpg>=0.29            # async driver behind SQLAlchemy's QueuePool
httpx>=0.27              # outbound calls between hops
redis>=5.0               # topic 4's cache
prometheus-client>=0.20  # /metrics, histograms not percentiles
